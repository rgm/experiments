#!/usr/bin/env bash

# clean
# Remove caches, build products

set -euo pipefail
cd "$(dirname "$0")/.."

if [ -f config.env ]; then
	. config.env
fi

rm -rf out .clj-kondo/cache .cljs_node_repl .cpcache .lsp .nrepl-port resources/public/js/* target
