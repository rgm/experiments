# conversation-loop

## getting started

1. Start VS Code + Calva or neovim + Conjure:
   - In calva: hit the `REPL ⚡` button (lower left) and "jack in". Use the deps.edn option.
   - In conjure: `:term script/nrepl`.
1. Run `(myapp.main/go)`.
4. Open `http://localhost:3000` in a browser
