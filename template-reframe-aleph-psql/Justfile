# run test suite
test:
	bin/kaocha

# start a repl
repl:
	clj -A:test:dev

# rebuild clj-kondo cache
lint-cache:
	clj-kondo --lint $(clojure -Spath)

# hunt down unused vars
carve:
	clojure --main carve.main --opts '{:paths ["src/clj" "test/clj"]}'

# vim: ft=make
