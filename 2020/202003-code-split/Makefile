dev:
	clojure -m figwheel.main -b dev -r

prd:
	clojure -m figwheel.main -O advanced -b dev

dev2:
	clojure -m figwheel.main -b dev2 -r

prd2:
	clojure -m figwheel.main -O advanced -b dev2

clean:
	rm -rf target
